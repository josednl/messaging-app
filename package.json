{
  "name": "messaging-app",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace root for messaging-app",
  "scripts": {
    "dev": "pnpm --filter \"@messaging-app/*\" --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.5.1",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
